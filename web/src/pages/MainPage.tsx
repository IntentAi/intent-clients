/**
 * MainPage - The main authenticated app page.
 * Contains the 3-panel layout (AppShell).
 */

import AppShell from '../components/layout/AppShell'

export default function MainPage() {
  return <AppShell />
}
